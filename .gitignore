node_modules/
out/
release/
dist/
test-results/
playwright-report/
coverage/
.playwright-mcp/
.vite/
*.log
*.tsbuildinfo

.env
.env.*
!.env.example
*.local

# Model files, sidecar binaries, recordings. Never committed.
*.onnx
*.gguf
*.enc

# Release signing keys. The private half lives in ~/.struq and never here;
# this is the second lock, not the first.
*.pem
*.key

.DS_Store
Thumbs.db

# Malformed temp paths created by local debug runs.
UsersRoyheAppDataLocalTempopencode*/
