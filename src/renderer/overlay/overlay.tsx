import type { JSX } from "react";

/**
 * The capture overlay pill. Phase 1 ports the full window lifecycle from
 * StruqADE and drives this surface through the capture states; until then it
 * renders nothing visible, because the overlay window is transparent.
 */
export function Overlay(): JSX.Element | null {
  return null;
}
